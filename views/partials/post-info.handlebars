<article class="post">
    <div class="title">
        <p>{{title}}</p>
    </div>
    <div class="meta">
        by {{user.username}} on {{createdAt}} |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
    </div>
</article>
{{!-- ADD Post body to this!! --}}